# เอกสารอธิบายเหตุผลการออกแบบ (Design Rationale)

## 1. แนวคิดหลัก (Design Concept)
* **Minimal & User-Centric:** เน้นความเรียบง่ายและสะอาดตา (Clean Interface) ตัดสิ่งรบกวนที่ไม่จำเป็นออก เพื่อให้ผู้ใช้โฟกัสไปที่การกรอกข้อมูลและดูผลลัพธ์ได้ทันที
* **Single Page Experience:** ออกแบบให้ฟังก์ชันหลัก (คำนวณ) และฟังก์ชันรอง (ดูประวัติ) อยู่ในหน้าเดียวกัน เพื่อให้ผู้ใช้เปรียบเทียบข้อมูลปัจจุบันกับอดีตได้ง่าย (Real-time Feedback) โดยไม่ต้องกดเปลี่ยนหน้าไปมา

## 2. การเลือกใช้สี (Color Palette)
* **สีหลัก (Primary Color):** 🟠 **สีส้มแดง (Orange-Red)**
    * ใช้สำหรับจุดที่ต้องการเน้น (Call-to-Action) เช่น ปุ่ม "CALCULATE" และโลโก้
    * **เหตุผล:** สื่อถึงพลังงาน (Energy), หัวใจ (Health), และความกระตือรือร้นในการดูแลสุขภาพ กระตุ้นให้ผู้ใช้รู้สึกตื่นตัว
* **สีสถานะ (Status Colors):** ใช้สีตามหลักสากลเพื่อให้เข้าใจง่าย (Universal Design)
    * 🟢 **สีเขียว (Normal):** สื่อถึงความปลอดภัยและสุขภาพดี
    * 🟡 **สีเหลือง (Under/Overweight):** สื่อถึงการเตือนให้ระวัง
    * 🔴 **สีแดง (Obese):** สื่อถึงอันตรายและต้องรีบแก้ไข
* **สีพื้นหลัง (Background):** ⚪ **สีขาว (White)** เพื่อให้อ่านตัวเลขและข้อความได้ชัดเจนที่สุด

## 3. การจัดวางองค์ประกอบ (Layout & UX)
* **Split-Screen Layout:** แบ่งหน้าจอส่วนคำนวณเป็น 2 ฝั่ง (ซ้าย-ขวา)
    * **ฝั่งซ้าย (Input):** รวมช่องกรอกข้อมูลไว้ด้วยกัน ทำให้ลำดับการกรอกลื่นไหล (Flow) จากบนลงล่าง
    * **ฝั่งขวา (Result):** แสดงผลลัพธ์ตัวเลขขนาดใหญ่ทันทีที่กดคำนวณ ช่วยให้ผู้ใช้เห็นภาพชัดเจน
* **Gender Selection:** ใช้การออกแบบปุ่มเลือกเพศแบบไอคอน (Icon Cards) ขนาดใหญ่ เพื่อความสวยงามและลดความผิดพลาดในการกด
* **History Table:** วางตารางประวัติไว้ด้านล่างสุด เพื่อไม่ให้รบกวนสายตาขณะคำนวณ แต่ยังสามารถเลื่อนลงมาดูข้อมูลย้อนหลังได้เมื่อต้องการ

## 4. รูปแบบตัวอักษร (Typography)
* เลือกใช้ฟอนต์แบบ **Sans-Serif** (ไม่มีหัว) ที่มีความทันสมัยและอ่านง่ายบนหน้าจอคอมพิวเตอร์และมือถือ
* เน้นขนาดตัวเลขผลลัพธ์ (BMI Value) ให้มีขนาดใหญ่พิเศษ (Extra Large) เพื่อให้เป็นจุดโฟกัสหลักของหน้าจอ
